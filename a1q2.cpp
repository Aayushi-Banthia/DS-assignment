#include <bitsstdc++.h>
using namespace std;

int main(){
cout<<"Consider an array and the target as the first element of the array.
create an empty hash array.
run a loop in the array from index 1 to n-1.
if there is no another element similar to the target in hash array then move that element value to hash array.
if there is a similar element present then skip that and move ahead.
when the loop is complete count the number of elements in hash array."<<endl;
return 0;
}